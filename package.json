{
  "name": "jira-issue-panel-custom-ui",
  "version": "1.0.10",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-plugin-react-hooks": "^4.2.0",
    "jest": "29.5.0"
  },
  "scripts": {
    "install-main": "cd static/main-app && npm run build",
    "install-wrapper": "cd static/wrapper-app && npm run build",
    "setup": "npm install && npm run install-main && npm run install-wrapper",
    "test":"jest"
  },
  "dependencies": {
    "@forge/api": "^2.3.0",
    "@forge/resolver": "^1.2.0",
    "openai": "3.1.0"
  }
}
